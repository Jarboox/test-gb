Para iniciar el servidor ejecutar los siguientes comandos en la terminal

#### `docker-compose build`
#### `docker-compose up -d`

Nota: Es necesario tener instalado docker y docker-compose en tu computadora